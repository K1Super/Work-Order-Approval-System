<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:flowable="http://flowable.org/bpmn"
             targetNamespace="http://www.flowable.org/processdef">

  <!--
    加班申请流程
    发起人 → 部门经理审批（核实加班真实性） → HR纳入考勤核算 → 结束
  -->
  <process id="overtime-process" name="加班申请流程" isExecutable="true">
    
    <startEvent id="start" name="提交加班申请" />
    
    <!-- 节点1: 部门经理审批（核实加班真实性与必要性） -->
    <userTask id="dept_manager_approve" name="部门经理审批" flowable:assignee="${deptManagerId}" />
    
    <!-- 节点2: HR核对并纳入考勤核算 -->
    <userTask id="hr_verify" name="HR考勤核算" flowable:assignee="${hrStaffId}" />
    
    <!-- 审批结果判断 -->
    <exclusiveGateway id="approve_gateway" name="审批结果" />
    
    <!-- 审批通过 - HR核算后结束 -->
    <endEvent id="end_approved" name="加班已批准并纳入考勤" />
    
    <!-- 审批拒绝 -->
    <endEvent id="end_rejected" name="已拒绝" />
    
    <!-- 流程连线 -->
    <sequenceFlow id="flow1" sourceRef="start" targetRef="dept_manager_approve" />
    <sequenceFlow id="flow2" sourceRef="dept_manager_approve" targetRef="approve_gateway" />
    
    <!-- 通过 -> HR考勤核算 -->
    <sequenceFlow id="flow3a" sourceRef="approve_gateway" targetRef="hr_verify">
      <conditionExpression xsi:type="tFormalExpression">${approved == true}</conditionExpression>
    </sequenceFlow>
    <sequenceFlow id="flow4a" sourceRef="hr_verify" targetRef="end_approved" />
    
    <!-- 拒绝 -->
    <sequenceFlow id="flow3b" sourceRef="approve_gateway" targetRef="end_rejected">
      <conditionExpression xsi:type="tFormalExpression">${approved == false}</conditionExpression>
    </sequenceFlow>
    
  </process>

</definitions>
