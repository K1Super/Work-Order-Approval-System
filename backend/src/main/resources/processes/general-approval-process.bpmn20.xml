<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:flowable="http://flowable.org/bpmn"
             targetNamespace="http://www.workorder.com/process">

    <!-- 简化的审批流程 -->
    <process id="general_approval_process" name="通用审批流程" isExecutable="true">

        <!-- 开始节点 -->
        <startEvent id="start" name="开始"/>

        <!-- 提交申请任务 -->
        <userTask id="submit_task" name="提交申请" flowable:assignee="${applicantId}"/>

        <!-- 部门经理审批 -->
        <userTask id="manager_approve" name="部门经理审批">
            <extensionElements>
                <flowable:taskListener event="create"
                        delegateExpression="${managerTaskListener}"/>
            </extensionElements>
        </userTask>

        <!-- 结束节点 -->
        <endEvent id="end" name="结束"/>

        <!-- 流程连线 -->
        <sequenceFlow id="flow1" sourceRef="start" targetRef="submit_task"/>
        <sequenceFlow id="flow2" sourceRef="submit_task" targetRef="manager_approve"/>
        <sequenceFlow id="flow3" sourceRef="manager_approve" targetRef="end"/>

    </process>

</definitions>
