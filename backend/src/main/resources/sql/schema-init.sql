-- ============================================================
-- 工单审批流转系统 — 初始化 Schema（已废弃）
-- ------------------------------------------------------------
-- 本文件已废弃，不再包含任何 DDL/DML。
--
-- Schema 事实源统一为 Flyway 迁移：
--   backend/src/main/resources/db/migration/V1__init.sql ~ V12__*.sql
--   由 V1~V12 全量管理表结构、种子数据、索引、分区与后续演进。
--
-- 保留本文件仅用于兼容历史启动路径：DatabaseInitializer.executeSchemaInit
-- 在库为空（Flyway 未就绪）的兜底场景下加载本文件；当前内容为空脚本，
-- 不会写入任何数据库对象，确保"Flyway 为唯一 Schema 事实源"。
--
-- 禁止在本文件恢复任何 DDL/DML；Schema 变更一律新增 Flyway 迁移文件。
-- ============================================================